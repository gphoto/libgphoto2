shared_module(
  'adc65',
  'adc65.c',
  dependencies: [
    libgphoto2_dep,
    config_dep,
  ],
  name_prefix: '',
  install: true,
  install_dir: camlibs_dir,
)

install_data(
  'README.adc65',
  install_dir: camlibdoc_dir,
  install_tag: 'doc',
)