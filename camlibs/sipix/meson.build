shared_module(
  'sipix_blink',
  'blink.c',
  dependencies: [
    libgphoto2_dep,
  ],
  name_prefix: '',
  install: true,
  install_dir: camlibs_dir,
)

shared_module(
  'sipix_blink2',
  'blink2.c',
  dependencies: [
    libgphoto2_dep,
    libjpeg_dep,
  ],
  name_prefix: '',
  install: true,
  install_dir: camlibs_dir,
)

shared_module(
  'sipix_web2',
  'web2.c',
  dependencies: [
    libgphoto2_dep,
  ],
  name_prefix: '',
  install: true,
  install_dir: camlibs_dir,
)